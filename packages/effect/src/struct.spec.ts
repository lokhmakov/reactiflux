import {describe, it} from 'vitest'
// import * as T from '@effect-ts/core/Effect'
// import * as Ref from '@effect-ts/core/Effect/Ref'
// import {pipe} from '@effect-ts/core/Function'

// const programm = T.struct({
//   first: T.succeed(1),
//   second: T.succeed(2),
// })

describe('struct', () => {
  it(`simple`, async () => {})
})
