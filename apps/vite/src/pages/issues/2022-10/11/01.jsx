// function Modal({modalStatus}: Props) {
//   const card = cards.find((item) => item.id === modalStatus.id)

//   return (
//     <Wrapper>
//       <ModalBody onMouseDown={(e) => e.nativeEvent.stopImmediatePropagation()}>
//         {card ? <div>{card.name}</div> : null}
//       </ModalBody>
//     </Wrapper>
//   )
// }
